#include<iostream>

using namespace std;
using std::cout;
using std::cin;
using std::endl;

void to_lower(char s[]);      //переводит строку в нижний регистр
void to_upper(char s[]);      //переводит строку в верхний регистр
void shrink(char s[]);        //удал€ет из строки лишние пробелы; ’орошо живЄт на свете ¬инни ѕух;
bool is_palindrome(char s[]); //ќпредел€ет, €вл€етс€ строка палиндромом
bool is_int_number(char s[]); //ќпредел€ет, €вл€етс€ ли строка целым числом
bool to_int_number(char s[]); //≈сли строка €вл€етс€ целым числом, то возврвщает его значение
bool is_bin_number(char s[]); //ќпредел€ет,€вл€етс€ ли строка двоичным числом
int bin_to_dec(char s[]);     //≈сли строка €вл€етс€ двоичным числом, возвращает его дес€тичное  значение
bool is_hex_number(char s[]); //ќпредел€ет €вл€етс€ ли строка шестнадцатиричным числом
int hex_to_dec(char s[]);     //≈сли строка €вл€етс€ шестнацатеричным числом, возвращает его дес€тиричное значение


void main()
{

}